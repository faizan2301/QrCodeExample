import {View, Text} from 'react-native';
import React from 'react';

const NavigationIcon = ({route, label}) => {
  return (
    <View>
      <Text>NavigationIcon</Text>
    </View>
  );
};

export default NavigationIcon;
